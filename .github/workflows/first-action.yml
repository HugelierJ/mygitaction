name: first Workflow
on:
  workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
    - name: Print hallo
      run: echo "Hello World"
    - name: Print daag
      run: echo "Goodbye"
